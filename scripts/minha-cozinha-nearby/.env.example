# Google Maps Platform API key with "Places API (New)" enabled.
# Copy this file to `.env.local` (which is gitignored) and fill in your key.
# Run the Google fetcher with:
#   node --env-file=scripts/minha-cozinha-nearby/.env.local scripts/minha-cozinha-nearby/fetch-google.ts
GOOGLE_MAPS_API_KEY=
