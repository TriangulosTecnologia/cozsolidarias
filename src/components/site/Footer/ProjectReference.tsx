import { Text } from '@chakra-ui/react';

/**
 * FAPESP project attribution text shown in the footer.
 *
 * @example
 * <ProjectReference />
 */
const ProjectReference = () => {
  return <Text fontSize="sm">Projeto Cozinhas Solidárias FAPESP</Text>;
};

export default ProjectReference;
