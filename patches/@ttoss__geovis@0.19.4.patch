diff --git a/dist/MapLibreAdapter-DBJCZI33.cjs b/dist/MapLibreAdapter-DBJCZI33.cjs
index 4864d3bfa2dab158f5b50da0c30a8c97806fcdc5..0a6b3c58e37ff2723ac48d73a0d0f608c4a7ff44 100644
--- a/dist/MapLibreAdapter-DBJCZI33.cjs
+++ b/dist/MapLibreAdapter-DBJCZI33.cjs
@@ -29,26 +29,31 @@ var collectManagedLayerIds = spec => {
 * Resolves the visibility to apply to a single `symbol` layer, or `null` when
 * the layer must be left untouched.
 *
-* - When labels are hidden, every symbol layer becomes `'none'`.
-* - When labels are shown, only basemap layers are restored to `'visible'`;
-*   managed (user) layers are skipped so their own visibility wins.
+* Managed (user) layers are skipped in **both** directions: `basemap.labels`
+* declares what the basemap draws, and a user symbol layer's own visibility is
+* declared by `layer.visible`. Hiding those too made the two settings
+* unreachable together — a map that hides place names to make room for its own
+* labels lost its own labels as well, with no field able to bring them back.
 */
 var resolveSymbolVisibility = (layerId, labelsVisible, managedIds) => {
-  if (!labelsVisible) return "none";
-  return managedIds.has(layerId) ? null : "visible";
+  if (managedIds.has(layerId)) return null;
+  return labelsVisible ? "visible" : "none";
 };
 /**
 * Applies `spec.basemap.labels` to the live map's `symbol` layers.
 *
-* - `labels === false` hides **every** `symbol` layer — basemap labels/icons
-*   AND any user-defined `symbol` layers — matching the historical
-*   `HideBasemapLabels` component behaviour.
-* - `labels === true` restores the basemap's own `symbol` layers to `visible`.
-*   User layers and their companions are skipped so their own visibility
-*   management (`layer.visible`, feature-state filters) is preserved.
+* - `labels === false` hides the basemap's own `symbol` layers (place, road and
+*   POI names/icons).
+* - `labels === true` restores those same layers to `visible`.
 * - `labels === undefined` leaves the style untouched, preserving the basemap's
 *   native label visibility.
 *
+* User layers and their companions are never touched, whichever way `labels`
+* points, so their own visibility management (`layer.visible`, feature-state
+* filters) is preserved. This is narrower than the historical
+* `HideBasemapLabels` component, which hid every `symbol` layer on the map:
+* hide `layer.visible` on a user symbol layer to get that back for one layer.
+*
 * Safe to call repeatedly and on every sync. When the style is still loading
 * (e.g. right after large GeoJSON sources are added on initial mount), setting
 * symbol visibility would be a no-op, so this re-schedules itself on the map's
@@ -282,6 +287,36 @@ var buildHeatmap = (base, _layer, paint, _ctx) => {
     }
   };
 };
+/**
+* Builds the `symbol` layer's `layout` block — the label text, size, fontstack
+* and icon. GeoVis carries these in the `paint` bag, which MapLibre splits
+* between `paint` and `layout`.
+*
+* `icon-image` is omitted rather than written as `undefined` when the layer
+* declares no icon. MapLibre validates `layout` on `addLayer` and rejects the
+* whole layer with `'undefined' value invalid, use null instead`, which aborts
+* the surrounding layer sync — `stripUndefinedPaint` cleans `paint` only, so a
+* text-only symbol layer would never reach the map.
+*
+* `text-font` is always written, defaulting to `Noto Sans Regular`. MapLibre's
+* own default fontstack (`Open Sans Regular`, `Arial Unicode MS Regular`) is
+* served by neither OpenFreeMap nor most OpenMapTiles-derived basemaps, so
+* leaving it unset 404s the glyph request and draws no text — a silent failure
+* that looks like the layer never mounted.
+*
+* Split out of `buildSymbol` so each stays under the complexity budget the
+* ESLint config enforces; `paint` and `layout` have no shared defaults.
+*/
+var buildSymbolLayout = sp => {
+  return {
+    "text-field": sp.textField ?? "",
+    "text-size": sp.textSize ?? 12,
+    "text-font": sp.textFont ?? ["Noto Sans Regular"],
+    ...(sp.iconImage === void 0 ? {} : {
+      "icon-image": sp.iconImage
+    })
+  };
+};
 /** Builds a MapLibre `symbol` layer spec from a GeoVis symbol layer. */
 var buildSymbol = (base, _layer, paint, _ctx) => {
   const sp = paint ?? {};
@@ -290,9 +325,7 @@ var buildSymbol = (base, _layer, paint, _ctx) => {
     type: "symbol",
     layout: {
       ...base.layout,
-      "text-field": sp.textField ?? "",
-      "text-size": sp.textSize ?? 12,
-      "icon-image": sp.iconImage
+      ...buildSymbolLayout(sp)
     },
     paint: {
       "text-color": sp.textColor ?? "#000000",
@@ -1621,6 +1654,26 @@ var applySourcePatch = (map, viewState, patch) => {
 //#endregion
 //#region src/adapters/maplibre/selection.ts
 /**
+* Builds the `setFeatureState` target, carrying `sourceLayer` when the layer
+* declares one. MapLibre requires it for vector sources: without it the call
+* fires `'The sourceLayer parameter must be provided for vector source types.'`
+* and writes nothing, so the `-selected-outline`/`-click-anchor` companions —
+* which filter on `feature-state.selected` — never light up on a tiled layer.
+* Resolved through `resolveSourceLayerFor`, the same helper those companion
+* layers are mounted with, so the write cannot address a different tile layer
+* than the one reading it.
+*/
+var featureStateTarget = entry => {
+  return entry.sourceLayer ? {
+    source: entry.sourceId,
+    sourceLayer: entry.sourceLayer,
+    id: entry.featureId
+  } : {
+    source: entry.sourceId,
+    id: entry.featureId
+  };
+};
+/**
 * Swaps `feature-state.selected` on one map: clears `prev` (if any), then
 * sets `next` (if any). Consolidates what the click hook (`useMapClick`)
 * used to do directly via `map.setFeatureState` before selection moved to
@@ -1628,37 +1681,33 @@ var applySourcePatch = (map, viewState, patch) => {
 * `dispatch({ type: 'select-feature' })` and any future selection source.
 */
 var swapSelectedFeatureState = (map, prev, next) => {
-  if (prev) map.setFeatureState({
-    source: prev.sourceId,
-    id: prev.featureId
-  }, {
+  if (prev) map.setFeatureState(featureStateTarget(prev), {
     selected: false
   });
-  if (next) map.setFeatureState({
-    source: next.sourceId,
-    id: next.featureId
-  }, {
+  if (next) map.setFeatureState(featureStateTarget(next), {
     selected: true
   });
 };
-/** Resolves a `GeoVisSelection`'s `sourceId` from its `layerId` via `spec.layers`. */
-var resolveSourceId = (spec, selection) => {
+/** Resolves the `VisualizationLayer` a `GeoVisSelection` points at. */
+var resolveLayer = (spec, selection) => {
   return spec.layers.find(l => {
     return l.id === selection.layerId;
-  })?.sourceId;
+  });
 };
 /**
 * Applies (or clears) the current selection on one mounted map, given the
-* spec it was mounted with (to resolve `layerId` → `sourceId`).
+* spec it was mounted with (to resolve `layerId` → `sourceId`/`sourceLayer`).
 */
 var applySelectionToMap = (map, spec, prev, next) => {
-  const prevSourceId = prev ? resolveSourceId(spec, prev) : void 0;
-  const nextSourceId = next ? resolveSourceId(spec, next) : void 0;
-  swapSelectedFeatureState(map, prev && prevSourceId ? {
-    sourceId: prevSourceId,
+  const prevLayer = prev ? resolveLayer(spec, prev) : void 0;
+  const nextLayer = next ? resolveLayer(spec, next) : void 0;
+  swapSelectedFeatureState(map, prev && prevLayer?.sourceId ? {
+    sourceId: prevLayer.sourceId,
+    sourceLayer: resolveSourceLayerFor(spec, prevLayer),
     featureId: prev.featureId
-  } : null, next && nextSourceId ? {
-    sourceId: nextSourceId,
+  } : null, next && nextLayer?.sourceId ? {
+    sourceId: nextLayer.sourceId,
+    sourceLayer: resolveSourceLayerFor(spec, nextLayer),
     featureId: next.featureId
   } : null);
 };
@@ -1871,13 +1920,16 @@ var destroyAll = views => {
 * Grounded in what `sourceTranslation.ts`/`layerTranslation.ts` actually
 * translate and what the package's test suite actually exercises — not
 * aspirational. `dataFeatures.featureState` is narrower than `sourceTypes`:
-* every source type mounts, but `setFeatureState` joining (`mapData`,
-* `sizeBy`) only works for `geojson`, whose features carry stable ids.
-* `dataFeatures.filter` (`VisualizationLayer.filter`, PRD-002) is declared
-* `geojson`-only for the same "declared means tested" reason, though
-* MapLibre's native `filter` works on any source with feature properties —
-* the narrower declaration reflects what's fixture-covered today, not an
-* engine limitation; it may widen once other source types are tested.
+* every source type mounts, but `setFeatureState` joining (`mapData`, and the
+* `sizeBy` path that reads through it) only works for `geojson`, whose
+* features carry stable ids — a `sizeBy` driven by `propertyName` needs no
+* join and is not covered by this entry.
+* `dataFeatures.filter` (`VisualizationLayer.filter`, PRD-002) covers
+* `vector-tiles` too: MapLibre's native `filter` reads `feature.properties`
+* on any source that carries them, and the `cluster-tiles` fixture
+* (`GeoVis/ClusterTiles`) exercises the compiled expression against a tiled
+* source, which is what the "declared means tested" bar asks for. The
+* remaining source types stay out until something covers them.
 * `pitch`/`bearing` are genuinely applied to the camera (see `applySetView`),
 * unlike the previous `supports3D: false` flag, which was dead and incorrect.
 */
@@ -1886,7 +1938,7 @@ var CAPABILITIES = {
   layerGeometries: ["polygon", "line", "point", "symbol", "heatmap", "raster"],
   dataFeatures: {
     featureState: ["geojson"],
-    filter: ["geojson"]
+    filter: ["geojson", "vector-tiles"]
   },
   viewFeatures: {
     pitch: true,
diff --git a/dist/MapLibreAdapter-RSNvmpAI.mjs b/dist/MapLibreAdapter-RSNvmpAI.mjs
index 4ea370ffaa30d5689e72187e353ee0a0c2f11716..ef915aaa7c2172e60184453d0f429486ce7d337c 100644
--- a/dist/MapLibreAdapter-RSNvmpAI.mjs
+++ b/dist/MapLibreAdapter-RSNvmpAI.mjs
@@ -28,26 +28,31 @@ var collectManagedLayerIds = spec => {
 * Resolves the visibility to apply to a single `symbol` layer, or `null` when
 * the layer must be left untouched.
 *
-* - When labels are hidden, every symbol layer becomes `'none'`.
-* - When labels are shown, only basemap layers are restored to `'visible'`;
-*   managed (user) layers are skipped so their own visibility wins.
+* Managed (user) layers are skipped in **both** directions: `basemap.labels`
+* declares what the basemap draws, and a user symbol layer's own visibility is
+* declared by `layer.visible`. Hiding those too made the two settings
+* unreachable together — a map that hides place names to make room for its own
+* labels lost its own labels as well, with no field able to bring them back.
 */
 var resolveSymbolVisibility = (layerId, labelsVisible, managedIds) => {
-  if (!labelsVisible) return "none";
-  return managedIds.has(layerId) ? null : "visible";
+  if (managedIds.has(layerId)) return null;
+  return labelsVisible ? "visible" : "none";
 };
 /**
 * Applies `spec.basemap.labels` to the live map's `symbol` layers.
 *
-* - `labels === false` hides **every** `symbol` layer — basemap labels/icons
-*   AND any user-defined `symbol` layers — matching the historical
-*   `HideBasemapLabels` component behaviour.
-* - `labels === true` restores the basemap's own `symbol` layers to `visible`.
-*   User layers and their companions are skipped so their own visibility
-*   management (`layer.visible`, feature-state filters) is preserved.
+* - `labels === false` hides the basemap's own `symbol` layers (place, road and
+*   POI names/icons).
+* - `labels === true` restores those same layers to `visible`.
 * - `labels === undefined` leaves the style untouched, preserving the basemap's
 *   native label visibility.
 *
+* User layers and their companions are never touched, whichever way `labels`
+* points, so their own visibility management (`layer.visible`, feature-state
+* filters) is preserved. This is narrower than the historical
+* `HideBasemapLabels` component, which hid every `symbol` layer on the map:
+* hide `layer.visible` on a user symbol layer to get that back for one layer.
+*
 * Safe to call repeatedly and on every sync. When the style is still loading
 * (e.g. right after large GeoJSON sources are added on initial mount), setting
 * symbol visibility would be a no-op, so this re-schedules itself on the map's
@@ -281,6 +286,36 @@ var buildHeatmap = (base, _layer, paint, _ctx) => {
     }
   };
 };
+/**
+* Builds the `symbol` layer's `layout` block — the label text, size, fontstack
+* and icon. GeoVis carries these in the `paint` bag, which MapLibre splits
+* between `paint` and `layout`.
+*
+* `icon-image` is omitted rather than written as `undefined` when the layer
+* declares no icon. MapLibre validates `layout` on `addLayer` and rejects the
+* whole layer with `'undefined' value invalid, use null instead`, which aborts
+* the surrounding layer sync — `stripUndefinedPaint` cleans `paint` only, so a
+* text-only symbol layer would never reach the map.
+*
+* `text-font` is always written, defaulting to `Noto Sans Regular`. MapLibre's
+* own default fontstack (`Open Sans Regular`, `Arial Unicode MS Regular`) is
+* served by neither OpenFreeMap nor most OpenMapTiles-derived basemaps, so
+* leaving it unset 404s the glyph request and draws no text — a silent failure
+* that looks like the layer never mounted.
+*
+* Split out of `buildSymbol` so each stays under the complexity budget the
+* ESLint config enforces; `paint` and `layout` have no shared defaults.
+*/
+var buildSymbolLayout = sp => {
+  return {
+    "text-field": sp.textField ?? "",
+    "text-size": sp.textSize ?? 12,
+    "text-font": sp.textFont ?? ["Noto Sans Regular"],
+    ...(sp.iconImage === void 0 ? {} : {
+      "icon-image": sp.iconImage
+    })
+  };
+};
 /** Builds a MapLibre `symbol` layer spec from a GeoVis symbol layer. */
 var buildSymbol = (base, _layer, paint, _ctx) => {
   const sp = paint ?? {};
@@ -289,9 +324,7 @@ var buildSymbol = (base, _layer, paint, _ctx) => {
     type: "symbol",
     layout: {
       ...base.layout,
-      "text-field": sp.textField ?? "",
-      "text-size": sp.textSize ?? 12,
-      "icon-image": sp.iconImage
+      ...buildSymbolLayout(sp)
     },
     paint: {
       "text-color": sp.textColor ?? "#000000",
@@ -1620,6 +1653,26 @@ var applySourcePatch = (map, viewState, patch) => {
 //#endregion
 //#region src/adapters/maplibre/selection.ts
 /**
+* Builds the `setFeatureState` target, carrying `sourceLayer` when the layer
+* declares one. MapLibre requires it for vector sources: without it the call
+* fires `'The sourceLayer parameter must be provided for vector source types.'`
+* and writes nothing, so the `-selected-outline`/`-click-anchor` companions —
+* which filter on `feature-state.selected` — never light up on a tiled layer.
+* Resolved through `resolveSourceLayerFor`, the same helper those companion
+* layers are mounted with, so the write cannot address a different tile layer
+* than the one reading it.
+*/
+var featureStateTarget = entry => {
+  return entry.sourceLayer ? {
+    source: entry.sourceId,
+    sourceLayer: entry.sourceLayer,
+    id: entry.featureId
+  } : {
+    source: entry.sourceId,
+    id: entry.featureId
+  };
+};
+/**
 * Swaps `feature-state.selected` on one map: clears `prev` (if any), then
 * sets `next` (if any). Consolidates what the click hook (`useMapClick`)
 * used to do directly via `map.setFeatureState` before selection moved to
@@ -1627,37 +1680,33 @@ var applySourcePatch = (map, viewState, patch) => {
 * `dispatch({ type: 'select-feature' })` and any future selection source.
 */
 var swapSelectedFeatureState = (map, prev, next) => {
-  if (prev) map.setFeatureState({
-    source: prev.sourceId,
-    id: prev.featureId
-  }, {
+  if (prev) map.setFeatureState(featureStateTarget(prev), {
     selected: false
   });
-  if (next) map.setFeatureState({
-    source: next.sourceId,
-    id: next.featureId
-  }, {
+  if (next) map.setFeatureState(featureStateTarget(next), {
     selected: true
   });
 };
-/** Resolves a `GeoVisSelection`'s `sourceId` from its `layerId` via `spec.layers`. */
-var resolveSourceId = (spec, selection) => {
+/** Resolves the `VisualizationLayer` a `GeoVisSelection` points at. */
+var resolveLayer = (spec, selection) => {
   return spec.layers.find(l => {
     return l.id === selection.layerId;
-  })?.sourceId;
+  });
 };
 /**
 * Applies (or clears) the current selection on one mounted map, given the
-* spec it was mounted with (to resolve `layerId` → `sourceId`).
+* spec it was mounted with (to resolve `layerId` → `sourceId`/`sourceLayer`).
 */
 var applySelectionToMap = (map, spec, prev, next) => {
-  const prevSourceId = prev ? resolveSourceId(spec, prev) : void 0;
-  const nextSourceId = next ? resolveSourceId(spec, next) : void 0;
-  swapSelectedFeatureState(map, prev && prevSourceId ? {
-    sourceId: prevSourceId,
+  const prevLayer = prev ? resolveLayer(spec, prev) : void 0;
+  const nextLayer = next ? resolveLayer(spec, next) : void 0;
+  swapSelectedFeatureState(map, prev && prevLayer?.sourceId ? {
+    sourceId: prevLayer.sourceId,
+    sourceLayer: resolveSourceLayerFor(spec, prevLayer),
     featureId: prev.featureId
-  } : null, next && nextSourceId ? {
-    sourceId: nextSourceId,
+  } : null, next && nextLayer?.sourceId ? {
+    sourceId: nextLayer.sourceId,
+    sourceLayer: resolveSourceLayerFor(spec, nextLayer),
     featureId: next.featureId
   } : null);
 };
@@ -1870,13 +1919,16 @@ var destroyAll = views => {
 * Grounded in what `sourceTranslation.ts`/`layerTranslation.ts` actually
 * translate and what the package's test suite actually exercises — not
 * aspirational. `dataFeatures.featureState` is narrower than `sourceTypes`:
-* every source type mounts, but `setFeatureState` joining (`mapData`,
-* `sizeBy`) only works for `geojson`, whose features carry stable ids.
-* `dataFeatures.filter` (`VisualizationLayer.filter`, PRD-002) is declared
-* `geojson`-only for the same "declared means tested" reason, though
-* MapLibre's native `filter` works on any source with feature properties —
-* the narrower declaration reflects what's fixture-covered today, not an
-* engine limitation; it may widen once other source types are tested.
+* every source type mounts, but `setFeatureState` joining (`mapData`, and the
+* `sizeBy` path that reads through it) only works for `geojson`, whose
+* features carry stable ids — a `sizeBy` driven by `propertyName` needs no
+* join and is not covered by this entry.
+* `dataFeatures.filter` (`VisualizationLayer.filter`, PRD-002) covers
+* `vector-tiles` too: MapLibre's native `filter` reads `feature.properties`
+* on any source that carries them, and the `cluster-tiles` fixture
+* (`GeoVis/ClusterTiles`) exercises the compiled expression against a tiled
+* source, which is what the "declared means tested" bar asks for. The
+* remaining source types stay out until something covers them.
 * `pitch`/`bearing` are genuinely applied to the camera (see `applySetView`),
 * unlike the previous `supports3D: false` flag, which was dead and incorrect.
 */
@@ -1885,7 +1937,7 @@ var CAPABILITIES = {
   layerGeometries: ["polygon", "line", "point", "symbol", "heatmap", "raster"],
   dataFeatures: {
     featureState: ["geojson"],
-    filter: ["geojson"]
+    filter: ["geojson", "vector-tiles"]
   },
   viewFeatures: {
     pitch: true,
diff --git a/dist/index.cjs b/dist/index.cjs
index b05e0051dfe377deae646556102468e1e7d477c1..c0a7b9f7dfda8aac7242ae8488ac2902c0a07eda 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -4558,6 +4558,44 @@ var coerceFeatureStateValue = raw => {
   return null;
 };
 /**
+* Reads the clicked/hovered feature's `feature-state`, addressing it the way
+* its source requires.
+*
+* A vector source needs `sourceLayer`: without it MapLibre fires
+* `'The sourceLayer parameter must be provided for vector source types.'` and
+* returns `undefined`, so the caller's `state.value` throws and the whole
+* handler dies — no tooltip, no click. The layer name is read off the queried
+* feature itself (`feature.layer['source-layer']`), which is where MapLibre
+* already resolved it. `?? {}` covers the remaining `undefined` returns (an
+* unknown source id, a feature with no id).
+*
+* @param map - The live map.
+* @param feature - The queried feature the state belongs to.
+* @param sourceId - Source the feature's layer reads from.
+* @returns The feature-state bag, `{}` when MapLibre could not address it.
+*
+* @example
+* ```typescript
+* const state = readFeatureState({ map, feature, sourceId });
+* const value = coerceFeatureStateValue(state.value);
+* ```
+*/
+var readFeatureState = ({
+  map,
+  feature,
+  sourceId
+}) => {
+  const sourceLayer = feature.layer && "source-layer" in feature.layer ? feature.layer["source-layer"] : void 0;
+  return (sourceLayer ? map.getFeatureState({
+    source: sourceId,
+    sourceLayer,
+    id: feature.id
+  }) : map.getFeatureState({
+    source: sourceId,
+    id: feature.id
+  })) ?? {};
+};
+/**
 * Decodes the `trackedKey` string (built in `useMapHover`'s `useMemo`) back
 * into a structured array so the `useEffect` body can consume it directly
 * without inline string-splitting logic.
@@ -4626,9 +4664,10 @@ var buildHandleMove = ({
         id: feature.id
       };
     } else prevHoveredState.current = null;
-    const state = map.getFeatureState({
-      source: sourceId,
-      id: feature.id
+    const state = readFeatureState({
+      map,
+      feature,
+      sourceId
     });
     lastPointRef.current = {
       x: event.point.x,
@@ -4700,9 +4739,10 @@ var buildHandleWindowFocus = ({
         id: feature.id
       };
     } else prevHoveredState.current = null;
-    const state = map.getFeatureState({
-      source: sourceId,
-      id: feature.id
+    const state = readFeatureState({
+      map,
+      feature,
+      sourceId
     });
     const rect = map.getCanvas().getBoundingClientRect();
     setHover({
@@ -4934,9 +4974,10 @@ var buildHandleClick = ({
       layerId: resolvedLayerId,
       featureId: feature.id
     });
-    const state = map.getFeatureState({
-      source: sourceId,
-      id: feature.id
+    const state = readFeatureState({
+      map,
+      feature,
+      sourceId
     });
     const featureLngLat = resolveFeatureLngLat({
       feature,
diff --git a/dist/index.d.cts b/dist/index.d.cts
index 5453ffdcb3fde39b4fe940c4c469484aa09ab21c..c97e41c2f5958d53ef2f8101d641b07d10785f0c 100644
--- a/dist/index.d.cts
+++ b/dist/index.d.cts
@@ -445,6 +445,19 @@ interface HeatmapPaint {
   /** Individual data point contribution weight. Default: 1. */
   heatmapWeight?: number;
 }
+/**
+ * A MapLibre expression: an operator followed by its arguments. Declared
+ * because the adapter hands `paint` straight to the style — the JSON schema
+ * types `layer.paint` as `additionalProperties: true`, so an expression both
+ * validates and reaches `text-field`/`text-size` intact. Only these two
+ * declarations were narrower than what the adapter already passed through.
+ *
+ * @example
+ * ```typescript
+ * const textField: SymbolExpression = ['get', 'count'];
+ * ```
+ */
+type SymbolExpression = [string, ...unknown[]];
 interface SymbolPaint {
   textColor?: string;
   textOpacity?: number;
@@ -452,8 +465,27 @@ interface SymbolPaint {
   textHaloWidth?: number;
   iconColor?: string;
   iconOpacity?: number;
-  textField?: string;
-  textSize?: number;
+  /**
+   * The label's text. A `{property}` token, a literal, or a
+   * {@link SymbolExpression} when the label has to be computed per feature
+   * (formatting a count, picking between properties).
+   */
+  textField?: string | SymbolExpression;
+  /**
+   * Label size in pixels, or a {@link SymbolExpression} to drive it from a
+   * feature property (`['step', ['get', 'count'], 11, 100, 14]`).
+   */
+  textSize?: number | SymbolExpression;
+  /**
+   * Fontstack the glyphs are requested from. Defaults to
+   * `['Noto Sans Regular']`, which OpenFreeMap and most OpenMapTiles-derived
+   * basemaps serve — MapLibre's own default (`Open Sans Regular`,
+   * `Arial Unicode MS Regular`) is served by neither, and a missing fontstack
+   * 404s the glyph request and rasterizes no text at all.
+   *
+   * @default ['Noto Sans Regular']
+   */
+  textFont?: string[];
   iconImage?: string;
 }
 type LayerPaint = FillPaint | LinePaint | CirclePaint | RasterPaint | HeatmapPaint | SymbolPaint;
@@ -1802,4 +1834,4 @@ declare const COMPACT_BREAKPOINT_PX = 640;
  */
 declare const useCompactViewport: () => boolean;
 //#endregion
-export { ActionLogEntry, BaseMapSpec, BoundaryGroup, BoundaryPaintOverrides, type BoundaryToggleResult, COMPACT_BREAKPOINT_PX, CONTEXT_PACKET_SCHEMA_VERSION, CapabilitySet, CategoricalColorBy, CategoricalColorByTemplate, CirclePaint, ColorBy, ColorByTemplate, ContextPacket, ContextPacketLayer, ContextPacketLegend, ContextPacketSource, ContextPacketViewPreset, DataSource, EngineAdapter, FillPaint, GeoJSONBoundingBox, GeoJSONFeature, GeoJSONFeatureCollection, GeoJSONGeometry, GeoJSONGeometryCollection, GeoJSONLineString, GeoJSONMultiLineString, GeoJSONMultiPoint, GeoJSONMultiPolygon, GeoJSONObject, GeoJSONPoint, GeoJSONPolygon, GeoJSONPosition, GeoJSONSource, GeoVisAction, GeoVisCanvas, GeoVisCanvasProps, GeoVisClickContext, GeoVisClickDismissContext, GeoVisContext, type GeoVisContextValue, GeoVisGeometryType, GeoVisHoverContext, GeoVisHoverTooltip, GeoVisHoverTooltipProps, GeoVisIssue, GeoVisIssueCode, GeoVisLayerControl, GeoVisLegend, GeoVisLegendProps, GeoVisMarker, GeoVisMarkerProps, GeoVisProvider, GeoVisResult, GeoVisResultStatus, GeoVisRuntime, GeoVisSelection, GeovisSpec, HeatmapPaint, type HoverTooltipConfig, ISSUE_CODE_STATUS, ImageSource, LabelFormatSpec, type LayerClickConfig, LayerControl, LayerControlItem, LayerFilter, LayerFilterOperator, LayerPaint, LayerTransition, LegendPosition, LegendSpec, LinePaint, LngLat, type MapClickInfo, MapData, MapDataRow, type MapHoverInfo, MapType, MountedView, NormalizationSpec, QuantitativeColorBy, QuantitativeColorByTemplate, RasterDemSource, RasterPaint, RasterTileSource, RepairOption, SEQUENTIAL_PALETTES, SPEC_SCHEMA_VERSION, SelectFeatureAction, SetFilterAction, SetMapDataAction, SetViewOptions, SetViewPresetAction, SizeBy, SpecPatch, SpecPatchTarget, SymbolPaint, ToggleLayerAction, type UseMapDataResult, VectorTileSource, VideoSource, ViewPreset, ViewState, VisualizationLayer, VisualizationSpec, VisualizationView, appendBoundaryGroup, buildContextPacket, createBoundaryGroup, createRuntime, customizeBoundaryGroup, formatCompactNumber, getBoundaryGroupId, parseReference, resolveOverallStatus, toggleBoundaryGroup, useBoundaryToggle, useCompactViewport, useDismissGeoVisClick, useGeoVis, useGeoVisClick, useGeoVisHover, useMapData, validateSpec };
\ No newline at end of file
+export { ActionLogEntry, BaseMapSpec, BoundaryGroup, BoundaryPaintOverrides, type BoundaryToggleResult, COMPACT_BREAKPOINT_PX, CONTEXT_PACKET_SCHEMA_VERSION, CapabilitySet, CategoricalColorBy, CategoricalColorByTemplate, CirclePaint, ColorBy, ColorByTemplate, ContextPacket, ContextPacketLayer, ContextPacketLegend, ContextPacketSource, ContextPacketViewPreset, DataSource, EngineAdapter, FillPaint, GeoJSONBoundingBox, GeoJSONFeature, GeoJSONFeatureCollection, GeoJSONGeometry, GeoJSONGeometryCollection, GeoJSONLineString, GeoJSONMultiLineString, GeoJSONMultiPoint, GeoJSONMultiPolygon, GeoJSONObject, GeoJSONPoint, GeoJSONPolygon, GeoJSONPosition, GeoJSONSource, GeoVisAction, GeoVisCanvas, GeoVisCanvasProps, GeoVisClickContext, GeoVisClickDismissContext, GeoVisContext, type GeoVisContextValue, GeoVisGeometryType, GeoVisHoverContext, GeoVisHoverTooltip, GeoVisHoverTooltipProps, GeoVisIssue, GeoVisIssueCode, GeoVisLayerControl, GeoVisLegend, GeoVisLegendProps, GeoVisMarker, GeoVisMarkerProps, GeoVisProvider, GeoVisResult, GeoVisResultStatus, GeoVisRuntime, GeoVisSelection, GeovisSpec, HeatmapPaint, type HoverTooltipConfig, ISSUE_CODE_STATUS, ImageSource, LabelFormatSpec, type LayerClickConfig, LayerControl, LayerControlItem, LayerFilter, LayerFilterOperator, LayerPaint, LayerTransition, LegendPosition, LegendSpec, LinePaint, LngLat, type MapClickInfo, MapData, MapDataRow, type MapHoverInfo, MapType, MountedView, NormalizationSpec, QuantitativeColorBy, QuantitativeColorByTemplate, RasterDemSource, RasterPaint, RasterTileSource, RepairOption, SEQUENTIAL_PALETTES, SPEC_SCHEMA_VERSION, SelectFeatureAction, SetFilterAction, SetMapDataAction, SetViewOptions, SetViewPresetAction, SizeBy, SpecPatch, SpecPatchTarget, SymbolExpression, SymbolPaint, ToggleLayerAction, type UseMapDataResult, VectorTileSource, VideoSource, ViewPreset, ViewState, VisualizationLayer, VisualizationSpec, VisualizationView, appendBoundaryGroup, buildContextPacket, createBoundaryGroup, createRuntime, customizeBoundaryGroup, formatCompactNumber, getBoundaryGroupId, parseReference, resolveOverallStatus, toggleBoundaryGroup, useBoundaryToggle, useCompactViewport, useDismissGeoVisClick, useGeoVis, useGeoVisClick, useGeoVisHover, useMapData, validateSpec };
\ No newline at end of file
diff --git a/dist/index.d.mts b/dist/index.d.mts
index 5453ffdcb3fde39b4fe940c4c469484aa09ab21c..c97e41c2f5958d53ef2f8101d641b07d10785f0c 100644
--- a/dist/index.d.mts
+++ b/dist/index.d.mts
@@ -445,6 +445,19 @@ interface HeatmapPaint {
   /** Individual data point contribution weight. Default: 1. */
   heatmapWeight?: number;
 }
+/**
+ * A MapLibre expression: an operator followed by its arguments. Declared
+ * because the adapter hands `paint` straight to the style — the JSON schema
+ * types `layer.paint` as `additionalProperties: true`, so an expression both
+ * validates and reaches `text-field`/`text-size` intact. Only these two
+ * declarations were narrower than what the adapter already passed through.
+ *
+ * @example
+ * ```typescript
+ * const textField: SymbolExpression = ['get', 'count'];
+ * ```
+ */
+type SymbolExpression = [string, ...unknown[]];
 interface SymbolPaint {
   textColor?: string;
   textOpacity?: number;
@@ -452,8 +465,27 @@ interface SymbolPaint {
   textHaloWidth?: number;
   iconColor?: string;
   iconOpacity?: number;
-  textField?: string;
-  textSize?: number;
+  /**
+   * The label's text. A `{property}` token, a literal, or a
+   * {@link SymbolExpression} when the label has to be computed per feature
+   * (formatting a count, picking between properties).
+   */
+  textField?: string | SymbolExpression;
+  /**
+   * Label size in pixels, or a {@link SymbolExpression} to drive it from a
+   * feature property (`['step', ['get', 'count'], 11, 100, 14]`).
+   */
+  textSize?: number | SymbolExpression;
+  /**
+   * Fontstack the glyphs are requested from. Defaults to
+   * `['Noto Sans Regular']`, which OpenFreeMap and most OpenMapTiles-derived
+   * basemaps serve — MapLibre's own default (`Open Sans Regular`,
+   * `Arial Unicode MS Regular`) is served by neither, and a missing fontstack
+   * 404s the glyph request and rasterizes no text at all.
+   *
+   * @default ['Noto Sans Regular']
+   */
+  textFont?: string[];
   iconImage?: string;
 }
 type LayerPaint = FillPaint | LinePaint | CirclePaint | RasterPaint | HeatmapPaint | SymbolPaint;
@@ -1802,4 +1834,4 @@ declare const COMPACT_BREAKPOINT_PX = 640;
  */
 declare const useCompactViewport: () => boolean;
 //#endregion
-export { ActionLogEntry, BaseMapSpec, BoundaryGroup, BoundaryPaintOverrides, type BoundaryToggleResult, COMPACT_BREAKPOINT_PX, CONTEXT_PACKET_SCHEMA_VERSION, CapabilitySet, CategoricalColorBy, CategoricalColorByTemplate, CirclePaint, ColorBy, ColorByTemplate, ContextPacket, ContextPacketLayer, ContextPacketLegend, ContextPacketSource, ContextPacketViewPreset, DataSource, EngineAdapter, FillPaint, GeoJSONBoundingBox, GeoJSONFeature, GeoJSONFeatureCollection, GeoJSONGeometry, GeoJSONGeometryCollection, GeoJSONLineString, GeoJSONMultiLineString, GeoJSONMultiPoint, GeoJSONMultiPolygon, GeoJSONObject, GeoJSONPoint, GeoJSONPolygon, GeoJSONPosition, GeoJSONSource, GeoVisAction, GeoVisCanvas, GeoVisCanvasProps, GeoVisClickContext, GeoVisClickDismissContext, GeoVisContext, type GeoVisContextValue, GeoVisGeometryType, GeoVisHoverContext, GeoVisHoverTooltip, GeoVisHoverTooltipProps, GeoVisIssue, GeoVisIssueCode, GeoVisLayerControl, GeoVisLegend, GeoVisLegendProps, GeoVisMarker, GeoVisMarkerProps, GeoVisProvider, GeoVisResult, GeoVisResultStatus, GeoVisRuntime, GeoVisSelection, GeovisSpec, HeatmapPaint, type HoverTooltipConfig, ISSUE_CODE_STATUS, ImageSource, LabelFormatSpec, type LayerClickConfig, LayerControl, LayerControlItem, LayerFilter, LayerFilterOperator, LayerPaint, LayerTransition, LegendPosition, LegendSpec, LinePaint, LngLat, type MapClickInfo, MapData, MapDataRow, type MapHoverInfo, MapType, MountedView, NormalizationSpec, QuantitativeColorBy, QuantitativeColorByTemplate, RasterDemSource, RasterPaint, RasterTileSource, RepairOption, SEQUENTIAL_PALETTES, SPEC_SCHEMA_VERSION, SelectFeatureAction, SetFilterAction, SetMapDataAction, SetViewOptions, SetViewPresetAction, SizeBy, SpecPatch, SpecPatchTarget, SymbolPaint, ToggleLayerAction, type UseMapDataResult, VectorTileSource, VideoSource, ViewPreset, ViewState, VisualizationLayer, VisualizationSpec, VisualizationView, appendBoundaryGroup, buildContextPacket, createBoundaryGroup, createRuntime, customizeBoundaryGroup, formatCompactNumber, getBoundaryGroupId, parseReference, resolveOverallStatus, toggleBoundaryGroup, useBoundaryToggle, useCompactViewport, useDismissGeoVisClick, useGeoVis, useGeoVisClick, useGeoVisHover, useMapData, validateSpec };
\ No newline at end of file
+export { ActionLogEntry, BaseMapSpec, BoundaryGroup, BoundaryPaintOverrides, type BoundaryToggleResult, COMPACT_BREAKPOINT_PX, CONTEXT_PACKET_SCHEMA_VERSION, CapabilitySet, CategoricalColorBy, CategoricalColorByTemplate, CirclePaint, ColorBy, ColorByTemplate, ContextPacket, ContextPacketLayer, ContextPacketLegend, ContextPacketSource, ContextPacketViewPreset, DataSource, EngineAdapter, FillPaint, GeoJSONBoundingBox, GeoJSONFeature, GeoJSONFeatureCollection, GeoJSONGeometry, GeoJSONGeometryCollection, GeoJSONLineString, GeoJSONMultiLineString, GeoJSONMultiPoint, GeoJSONMultiPolygon, GeoJSONObject, GeoJSONPoint, GeoJSONPolygon, GeoJSONPosition, GeoJSONSource, GeoVisAction, GeoVisCanvas, GeoVisCanvasProps, GeoVisClickContext, GeoVisClickDismissContext, GeoVisContext, type GeoVisContextValue, GeoVisGeometryType, GeoVisHoverContext, GeoVisHoverTooltip, GeoVisHoverTooltipProps, GeoVisIssue, GeoVisIssueCode, GeoVisLayerControl, GeoVisLegend, GeoVisLegendProps, GeoVisMarker, GeoVisMarkerProps, GeoVisProvider, GeoVisResult, GeoVisResultStatus, GeoVisRuntime, GeoVisSelection, GeovisSpec, HeatmapPaint, type HoverTooltipConfig, ISSUE_CODE_STATUS, ImageSource, LabelFormatSpec, type LayerClickConfig, LayerControl, LayerControlItem, LayerFilter, LayerFilterOperator, LayerPaint, LayerTransition, LegendPosition, LegendSpec, LinePaint, LngLat, type MapClickInfo, MapData, MapDataRow, type MapHoverInfo, MapType, MountedView, NormalizationSpec, QuantitativeColorBy, QuantitativeColorByTemplate, RasterDemSource, RasterPaint, RasterTileSource, RepairOption, SEQUENTIAL_PALETTES, SPEC_SCHEMA_VERSION, SelectFeatureAction, SetFilterAction, SetMapDataAction, SetViewOptions, SetViewPresetAction, SizeBy, SpecPatch, SpecPatchTarget, SymbolExpression, SymbolPaint, ToggleLayerAction, type UseMapDataResult, VectorTileSource, VideoSource, ViewPreset, ViewState, VisualizationLayer, VisualizationSpec, VisualizationView, appendBoundaryGroup, buildContextPacket, createBoundaryGroup, createRuntime, customizeBoundaryGroup, formatCompactNumber, getBoundaryGroupId, parseReference, resolveOverallStatus, toggleBoundaryGroup, useBoundaryToggle, useCompactViewport, useDismissGeoVisClick, useGeoVis, useGeoVisClick, useGeoVisHover, useMapData, validateSpec };
\ No newline at end of file
diff --git a/dist/index.mjs b/dist/index.mjs
index d919ea26f64840cccd5d7de5110684410b6bce1a..3087b53eb7c3b0d293ee10c754b71775fdd56a57 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -4552,6 +4552,44 @@ var coerceFeatureStateValue = raw => {
   return null;
 };
 /**
+* Reads the clicked/hovered feature's `feature-state`, addressing it the way
+* its source requires.
+*
+* A vector source needs `sourceLayer`: without it MapLibre fires
+* `'The sourceLayer parameter must be provided for vector source types.'` and
+* returns `undefined`, so the caller's `state.value` throws and the whole
+* handler dies — no tooltip, no click. The layer name is read off the queried
+* feature itself (`feature.layer['source-layer']`), which is where MapLibre
+* already resolved it. `?? {}` covers the remaining `undefined` returns (an
+* unknown source id, a feature with no id).
+*
+* @param map - The live map.
+* @param feature - The queried feature the state belongs to.
+* @param sourceId - Source the feature's layer reads from.
+* @returns The feature-state bag, `{}` when MapLibre could not address it.
+*
+* @example
+* ```typescript
+* const state = readFeatureState({ map, feature, sourceId });
+* const value = coerceFeatureStateValue(state.value);
+* ```
+*/
+var readFeatureState = ({
+  map,
+  feature,
+  sourceId
+}) => {
+  const sourceLayer = feature.layer && "source-layer" in feature.layer ? feature.layer["source-layer"] : void 0;
+  return (sourceLayer ? map.getFeatureState({
+    source: sourceId,
+    sourceLayer,
+    id: feature.id
+  }) : map.getFeatureState({
+    source: sourceId,
+    id: feature.id
+  })) ?? {};
+};
+/**
 * Decodes the `trackedKey` string (built in `useMapHover`'s `useMemo`) back
 * into a structured array so the `useEffect` body can consume it directly
 * without inline string-splitting logic.
@@ -4620,9 +4658,10 @@ var buildHandleMove = ({
         id: feature.id
       };
     } else prevHoveredState.current = null;
-    const state = map.getFeatureState({
-      source: sourceId,
-      id: feature.id
+    const state = readFeatureState({
+      map,
+      feature,
+      sourceId
     });
     lastPointRef.current = {
       x: event.point.x,
@@ -4694,9 +4733,10 @@ var buildHandleWindowFocus = ({
         id: feature.id
       };
     } else prevHoveredState.current = null;
-    const state = map.getFeatureState({
-      source: sourceId,
-      id: feature.id
+    const state = readFeatureState({
+      map,
+      feature,
+      sourceId
     });
     const rect = map.getCanvas().getBoundingClientRect();
     setHover({
@@ -4928,9 +4968,10 @@ var buildHandleClick = ({
       layerId: resolvedLayerId,
       featureId: feature.id
     });
-    const state = map.getFeatureState({
-      source: sourceId,
-      id: feature.id
+    const state = readFeatureState({
+      map,
+      feature,
+      sourceId
     });
     const featureLngLat = resolveFeatureLngLat({
       feature,
